# Python driver

```bash
$ ./driver.py PCAP_FILE
```

## Requirements

* Python 3  (Tested with 3.6.)
* [Scapy](https://scapy.net/) library

Install the required dependencies using:

```bash
$ pip install -r requirements.txt
```

## Solution guidelines

Parse DNS packages and do some statistic.

